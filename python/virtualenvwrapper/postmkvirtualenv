#!/bin/zsh
# This hook is sourced after a new virtualenv is activated.


if [[ -z $LOCALVENV && $PWD =~ $HOME/rp ]]; then
    pip install --pre --upgrade -r requirements/flake8.txt -e .\
        flake8-breakpoint flake8-mutable flake8-variables-names flake8-bugbear \
        ipython ipdb flake8
fi
